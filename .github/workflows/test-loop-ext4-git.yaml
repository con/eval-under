name: Loop ext4 / git testsuite

# GENERATED FILE -- edit bin/ci/matrix.sh and re-run
# bin/ci/gen-dispatchers.sh instead of editing this by hand.
#
# Thin dispatcher so this matrix cell (loop-ext4 x git) gets a distinct
# README badge. All work is delegated to _test-under.yaml (reusable).

on:
  push:
    branches: [main, master]
    paths:
      - "bin/**"
      - "provision/**"
      - ".github/workflows/_test-under.yaml"
      - ".github/workflows/test-loop-ext4-git.yaml"
  pull_request:
    paths:
      - "bin/**"
      - "provision/**"
      - ".github/workflows/_test-under.yaml"
      - ".github/workflows/test-loop-ext4-git.yaml"
  workflow_dispatch:
  schedule:
    - cron: "32 5 * * 1"  # Mon 05:32 UTC

concurrency:
  group: loop-ext4-git-${{ github.ref }}
  cancel-in-progress: true

jobs:
  call:
    uses: ./.github/workflows/_test-under.yaml
    with:
      backend: "loop"
      backend-version: "ext4"
      target: "git"
    permissions:
      contents: read
      actions: read

name: BeeGFS 8.1.0 / pjdfstest

# GENERATED FILE -- edit bin/ci/matrix.sh and re-run
# bin/ci/gen-dispatchers.sh instead of editing this by hand.
#
# Thin dispatcher so this matrix cell (beegfs-8.1.0 x pjdfstest) gets a distinct
# README badge. All work is delegated to _test-under.yaml (reusable).

on:
  push:
    branches: [main, master]
    paths:
      - "bin/**"
      - "fixtures/beegfs/**"
      - "provision/**"
      - ".github/workflows/_test-under.yaml"
      - ".github/workflows/test-beegfs-8.1.0-pjdfstest.yaml"
  pull_request:
    paths:
      - "bin/**"
      - "fixtures/beegfs/**"
      - "provision/**"
      - ".github/workflows/_test-under.yaml"
      - ".github/workflows/test-beegfs-8.1.0-pjdfstest.yaml"
  workflow_dispatch:
  schedule:
    - cron: "2 5 * * 1"  # Mon 05:02 UTC

concurrency:
  group: beegfs-8.1.0-pjdfstest-${{ github.ref }}
  cancel-in-progress: true

jobs:
  call:
    uses: ./.github/workflows/_test-under.yaml
    with:
      backend: "beegfs"
      backend-version: "8.1.0"
      target: "pjdfstest"
    permissions:
      contents: read
      actions: read
